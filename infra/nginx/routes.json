[
  {
    "path": "/",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Default catch-all (plan §1.5: php_backend is the strangler default). Required — the generator refuses to render without a '/' entry."
  },
  {
    "path": "/miniapp",
    "upstream": "next_miniapp",
    "tenants": "all",
    "note": "line-mini-app is already the production LIFF experience today (see CLAUDE.md) — this route documents current reality, not a future flip."
  },
  {
    "path": "/ws",
    "upstream": "ws",
    "tenants": "all",
    "note": "Realtime inbox updates (websocket-server.js today; superseded by apps/worker's unified websocket in Phase 4)."
  },
  {
    "path": "/admin-preview",
    "upstream": "next_admin",
    "tenants": ["demo-tenant"],
    "note": "Working example of a per-tenant canary route (plan §7.3 ramp: demo tenant first). apps/admin doesn't exist until Phase 1 — this entry is a placeholder demonstrating the round-trip add/remove mechanics mig-verify checks, not a live flip."
  },
  {
    "path": "/users",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 1 (mig-ui users-pages brief): Next port of users.php's LINE tab lives at apps/admin's /users, parity-proven by infra/e2e/parity.mjs. Schema-valid 'not yet flipped' placeholder (php_backend is already the default, so this is a functional no-op) — mig-orchestrator flips upstream to next_admin only after a completed canary ramp (plan §7.3: demo tenant -> 1 real tenant -> 10% -> 50% -> 100%), never this agent."
  },
  {
    "path": "/user-detail",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 1 (mig-ui users-pages brief): Next port of user-detail.php lives at apps/admin's /user-detail?id=N (same query-param URL shape), parity-proven by infra/e2e/parity.mjs. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/dashboard",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 1 (mig-ui dashboard brief): Next port of dashboard.php (tabs executive/crm) lives at apps/admin's /dashboard — NOT '/', which stays the public storefront (index.php, the pre-existing catch-all above; see docs/runbooks/phase2-batch1-users-dashboard-parity.md for why). Parity-proven by infra/e2e/parity.mjs. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/analytics",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 2 (mig-ui analytics brief): Next port of analytics.php (tabs overview/advanced/crm/account) lives at apps/admin's /analytics, parity-proven by infra/e2e/parity.mjs. Schema-valid 'not yet flipped' placeholder (php_backend is already the default, so this is a functional no-op) — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/activity-logs",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 2 (mig-ui analytics brief): Next port of activity-logs.php lives at apps/admin's /activity-logs, parity-proven by infra/e2e/parity.mjs. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/loyalty-members",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 2 (mig-ui analytics brief): Next port of loyalty-members.php lives at apps/admin's /loyalty-members, parity-proven by infra/e2e/parity.mjs. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/crm-dashboard-advanced",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 3 (mig-ui crm-dashboard-advanced/system-status brief): Next port of crm-dashboard-advanced.php lives at apps/admin's /crm-dashboard-advanced, parity-proven by infra/e2e/parity.mjs's runCrmDashboardAdvancedChecks() (a deliberate exception to the harness's usual PHP-vs-Next diff — PHP 500s on crm_deals/crm_tickets absent from the committed tenant schema, Next returns 200 with an authorized defensive-empty fallback; see docs/runbooks/phase2-batch1-users-dashboard-parity.md's 'Phase 2 batch 3' section). Schema-valid 'not yet flipped' placeholder (php_backend is already the default, so this is a functional no-op) — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/system-status",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 3 (mig-ui crm-dashboard-advanced/system-status brief): Next port of system-status.php lives at apps/admin's /system-status, parity-proven by infra/e2e/parity.mjs's system-status:baseline entry (11 value-diffed portable checks + 8 presence-only placeholder checks — see extractSystemStatusPage() in infra/e2e/lib/extract.mjs and the runbook's 'Phase 2 batch 3' section for the split). Unrelated to and non-conflicting with parity.mjs's own PROBE_PATH, which fetches /system-status.php UNAUTHENTICATED against the PHP container directly, purely to trigger admin_users' auto-create sequencing before this batch's page-pair entries ever run. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/templates",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 3 (mig-ui templates/groups brief): Next port of templates.php lives at apps/admin's /templates, parity-proven by infra/e2e/parity.mjs's templates:baseline entry. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/groups",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 3 (mig-ui templates/groups brief): Next port of groups.php (incl. its ?view=<id> detail panel) lives at apps/admin's /groups?view=<id> (same query-param URL shape), parity-proven by infra/e2e/parity.mjs's groups:baseline|view-empty|view-members entries. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/line-groups",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 3 (mig-ui line-groups brief): Next port of line-groups.php lives at apps/admin's /line-groups, parity-proven by infra/e2e/parity.mjs's line-groups:baseline entry. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/line-group-detail",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 2 batch 3 (mig-ui line-groups brief): Next port of line-group-detail.php lives at apps/admin's /line-group-detail?id=N (same query-param URL shape, a SEPARATE top-level route from /line-groups per the pagesB brief's grep-verified ?id= finding, matching /user-detail?id=N's established precedent), parity-proven by infra/e2e/parity.mjs's line-group-detail:id=1|2 entries. Schema-valid 'not yet flipped' placeholder — see the /users entry's note for the flip mechanic and who owns it."
  },
  {
    "path": "/api/miniapp",
    "upstream": "next_admin",
    "tenants": "all",
    "note": "Phase 3 batch 1 (mig-api-reads/mig-api-writes briefs; parity-proven by infra/e2e/api-parity.mjs): line-mini-app's public JSON API surface (resolve-line-account, points-history, shop-products, health-profile, member, rewards, wishlist), ported to apps/admin's /api/miniapp/** Route Handlers. DELIBERATE DEVIATION from the prior phase-2 'default php_backend, schema-valid not-yet-flipped placeholder' convention: /api/miniapp/** is a BRAND NEW path PHP has never served — the ported endpoints reuse legacy paths like /api/member.php only as the origin-map fallback line-mini-app's client-side php-bridge.ts override targets, they do not live at those literal paths in apps/admin. A php_backend placeholder is therefore meaningless here (nothing on that upstream serves this path), unlike /users, /dashboard, etc. which mirror an existing PHP page 1:1 — so this entry points directly at next_admin, unconditionally, for every tenant. The actual canary ramp (demo tenant -> 1 tenant -> 10% -> 50% -> 100%) for this surface is NOT driven by this file at all — line-mini-app is one static bundle shared by every tenant, so a host×path nginx flip can't express a traffic percentage the way it can for a per-tenant-hostname admin page. mig-orchestrator instead ramps traffic client-side via line-mini-app's NEXT_PUBLIC_MINIAPP_ENDPOINT_OVERRIDES env/config (php-bridge.ts's per-endpoint override map) — see docs/runbooks/phase3-batch1-miniapp-api-parity.md. This specific routing decision is mig-orchestrator's explicit, already-given sign-off (documented, not a question for mig-verify to re-litigate)."
  },
  {
    "path": "/inbox",
    "upstream": "php_backend",
    "tenants": "all",
    "note": "Phase 4 batch 1 (mig-api conversationList brief + mig-ui messageThread brief): Next port of inbox-v2.php's conversation-list sidebar + chat-thread pane (reads only) lives at apps/admin's /inbox (list) and /inbox/[userId] (thread, a dynamic segment — NOT the PHP page's own ?user=N query-param shape, see the runbook), backed by the new /api/inbox/conversations and /api/inbox/messages cursor-pagination Route Handlers. Parity-proven by infra/e2e/parity.mjs's inbox-conversations-cursor-walk / inbox-messages-cursor-walk / inbox:php-empty-currentbotid-clobbered / inbox:next-baseline / inbox-thread:id=N entries — see docs/runbooks/phase4-batch1-inbox-reads-parity.md for the full contract, the identity-model decision (parity.mjs's tenant-Host+session-cookie model, not api-parity.mjs's unauthenticated root-domain model — this surface is tenant-scoped and admin-authenticated like /users, not line_account_id-resolved like /api/miniapp), and the deferred-scope list. Schema-valid 'not yet flipped' placeholder (php_backend is already the default, so this is a functional no-op) — see the /users entry's note for the flip mechanic and who owns it. THIS BATCH IS READS ONLY: inbox-v2.php exposes ~29 actions (send/dispense/tag/note/assign/mark-as-read/AI-copilot/...); only 2 read actions (getConversations/getMessages) are ported so far, landing ~5 more at a time in later batches per the migration plan's phasing. Do NOT treat this placeholder as flip-ready — mig-orchestrator will not start a canary ramp for /inbox until enough of those later batches land to make the Next page a usable replacement for the PHP inbox, and only mig-orchestrator (never this batch's executor) decides when that ramp starts."
  }
]
