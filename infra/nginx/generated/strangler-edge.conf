# =====================================================================
# AUTO-GENERATED by infra/nginx/generate-routes.mjs — DO NOT EDIT BY HAND
#
# Source manifest : infra/nginx/routes.json
# Generated at    : 2026-08-14T08:00:53.097Z
# Regenerate      : node infra/nginx/generate-routes.mjs
#
# Route flip / rollback mechanism (plan §1.5): edit one entry in
# routes.json, re-run the generator, reload nginx. Rolling a route back is
# reverting that one JSON line + regenerate + reload — nothing else in this
# file needs to change.
# =====================================================================

worker_processes auto;
events {
    worker_connections 1024;
}

http {
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;

    # Docker embedded DNS — lets the edge resolve upstream service names at
    # REQUEST time (via the $svc_addr variable below) instead of at config
    # load time. This is what lets nginx -t / reload succeed even if a
    # backend container is briefly down or not yet started, and is required
    # for the two-tier host×path canary maps to pick a different upstream
    # per request without needing separate static upstream{} blocks.
    resolver 127.0.0.11 valid=10s ipv6=off;

    # WebSocket Connection header helper (Upgrade -> "upgrade", else "close").
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    # --- Tier 0: upstream name -> service address. Edit topology here, not
    #     per-route. ---
    map $upstream_name $svc_addr {
    php_backend   php:80;
    next_admin    next-admin:3000;
    next_miniapp  next-miniapp:3000;
    ws            ws:3001;
        default       php:80;
    }

    # X-Served-By: php|next (plan §1.5: "ทุก response ติด X-Served-By: php|next")
    map $upstream_name $served_by {
    php_backend   php;
    default       next;
    }

    # --- Tier 1: host -> tenant slug. Production hostnames are
    #     tenant-XXXX.re-ya.com (subdomain routing, ADR-001); anything else
    #     (root domain, reserved subdomains) has no tenant scope. ---
    map $host $tenant_slug {
        ~^tenant-(?<slug>[a-z0-9-]+)\.re-ya\.com$   $slug;
        default                                       "";
    }

    # --- Tier 2: per-route tenant -> upstream maps (only emitted for routes
    #     with an explicit per-tenant canary list; "all" routes are a plain
    #     `set` in their location block instead). ---
    # route: /admin-preview -> next_admin (per-tenant canary: ["demo-tenant"])
    # note: Working example of a per-tenant canary route (plan §7.3 ramp: demo tenant first). apps/admin doesn't exist until Phase 1 — this entry is a placeholder demonstrating the round-trip add/remove mechanics mig-verify checks, not a live flip.
    map $tenant_slug $upstream_admin_preview {
        demo-tenant    next_admin;
        default    php_backend;
    }

    server {
        listen 80;
        server_name _;

        # --- Ported from repo-root .htaccess: security headers ---
        # (edge is authoritative — locations below proxy_hide_header the
        # same names so nothing duplicates them).
        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options SAMEORIGIN always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header X-Served-By $served_by always;

        # --- Ported from .htaccess: block ad-hoc / one-off debug & data-fix
        #     scripts at web root (check_*, debug_*, fix_*, final_*, sync_*,
        #     get_*, verify_*). Root-anchored, matches the .htaccess intent
        #     exactly (api/sync_*.php etc. are NOT matched). ---
        location ~ ^/(check_|debug_|fix_|final_|sync_|get_|verify_)[^/]*\.php$ {
            return 403;
        }

        # --- Ported from .htaccess: liff-*.php -> /miniapp/ redirects.
        #     302 (not 301) so clients don't cache the mapping while
        #     channels migrate — QSA-equivalent via $is_args$args. ---
    location = /liff-app.php { return 302 /miniapp/$is_args$args; }
    location = /liff-appointment.php { return 302 /miniapp/appointments/$is_args$args; }
    location = /liff-my-appointments.php { return 302 /miniapp/appointments/$is_args$args; }
    location = /liff-checkout.php { return 302 /miniapp/cart/$is_args$args; }
    location = /liff-member-card.php { return 302 /miniapp/profile/$is_args$args; }
    location = /liff-settings.php { return 302 /miniapp/profile/$is_args$args; }
    location = /liff-my-orders.php { return 302 /miniapp/orders/$is_args$args; }
    location = /liff-order-detail.php { return 302 /miniapp/orders/$is_args$args; }
    location = /liff-pharmacy-consult.php { return 302 /miniapp/ai-chat/$is_args$args; }
    location = /liff-symptom-assessment.php { return 302 /miniapp/ai-chat/$is_args$args; }
    location = /liff-points-history.php { return 302 /miniapp/rewards/history/$is_args$args; }
    location = /liff-points-rules.php { return 302 /miniapp/rewards/$is_args$args; }
    location = /liff-redeem-points.php { return 302 /miniapp/rewards/$is_args$args; }
    location = /liff-product-detail.php { return 302 /miniapp/shop/$is_args$args; }
    location = /liff-promotions.php { return 302 /miniapp/shop/$is_args$args; }
    location = /liff-shop.php { return 302 /miniapp/shop/$is_args$args; }
    location = /liff-shop-v3.php { return 302 /miniapp/shop/$is_args$args; }
    location = /liff-register.php { return 302 /miniapp/register/$is_args$args; }
    location = /liff-video-call.php { return 302 /miniapp/video/$is_args$args; }
    location = /liff-video-call-pro.php { return 302 /miniapp/video/$is_args$args; }
    location = /liff-wishlist.php { return 302 /miniapp/wishlist/$is_args$args; }
    location = /liff-consent.php { return 302 /miniapp/$is_args$args; }
    location = /liff-main.php { return 302 /miniapp/$is_args$args; }
        # liff-share.php has no Mini App equivalent yet — intentionally
        # left off this list, same as .htaccess (stays on legacy /liff/).

        # Health check for the compose healthcheck / smoke tests.
        location = /__edge-health {
            access_log off;
            default_type text/plain;
            return 200 "edge-ok\n";
        }

        # --- Route manifest locations (from routes.json) ---
    location / {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /miniapp {
        set $upstream_name next_miniapp;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /ws {
        set $upstream_name ws;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /admin-preview {
        set $upstream_name $upstream_admin_preview;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /users {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /user-detail {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /dashboard {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /analytics {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /activity-logs {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /loyalty-members {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /crm-dashboard-advanced {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /system-status {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /templates {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /groups {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /line-groups {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /line-group-detail {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /api/miniapp {
        set $upstream_name next_admin;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /inbox {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /articles {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /article {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /pharmacists {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /settings {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /api/documents {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /shop/orders {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /shop/order-detail {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }

    location /broadcast {
        set $upstream_name php_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        # WebSocket upgrade support (harmless no-op for http upstreams).
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Edge is authoritative for these — strip anything the upstream
        # already set so we never emit duplicates.
        proxy_hide_header X-Frame-Options;
        proxy_hide_header X-Content-Type-Options;
        proxy_hide_header X-XSS-Protection;

        proxy_pass http://$svc_addr;
    }
    }
}
