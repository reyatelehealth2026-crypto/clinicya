{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "description": "Long-lived Node worker process — BullMQ queues, typed job registry, tenant-fanout scaffolding, DLQ, health endpoint, graceful drain. Pure scaffolding batch for docs/plans/2026-07-12-nextjs-full-migration-plan.md §1.1 / Phase 8 / Phase 10 — no real cron-job business logic ported yet.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@reya/config": "workspace:*",
    "@reya/db": "workspace:*",
    "@reya/tenant": "workspace:*",
    "bullmq": "^5.80.2",
    "ioredis": "5.10.1",
    "kysely": "^0.29.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
